import { ReactElement } from "react";
import { Button } from "../components/ui/button";

const LandingPage = (): ReactElement => {
    return (
        <div>
            LandingPage <Button>Hello</Button>
        </div>
    );
};
export default LandingPage;
